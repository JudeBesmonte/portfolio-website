/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['aygaepypiusloubktinn.supabase.co'],
  },
  experimental: {
    serverActions: true,
  },
}

module.exports = nextConfig